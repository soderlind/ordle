{
  "private": true,
  "name": "ordle",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build && cp -rf ./build/. ../ordle.github.io",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "preact watch",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "preact"
  },
  "eslintIgnore": [
    "docs/*"
  ],
  "devDependencies": {
    "eslint": "^8.16.0",
    "eslint-config-preact": "^1.3.0",
    "glob-parent": ">=5.1.2",
    "nth-check": ">=2.0.1",
    "preact-cli": "^2.2.1",
    "sirv-cli": "^2.0.2"
  },
  "dependencies": {
    "glob-parent": ">=5.1.2",
    "nth-check": ">=2.0.1",
    "preact": "^10.7.2",
    "preact-render-to-string": "^5.2.0"
  }
}
